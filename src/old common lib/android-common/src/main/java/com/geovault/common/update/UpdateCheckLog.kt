package com.geovault.common.update

/**
 * Log tag for app update / Gitea release checks. Filter logcat with: `adb logcat -s GeoVaultUpdateCheck`.
 */
internal object UpdateCheckLog {
    const val TAG = "GeoVaultUpdateCheck"
}
