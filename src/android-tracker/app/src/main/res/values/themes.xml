<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Platform has no android:Theme.Material.DayNight.*; AppCompat provides DayNight + NoActionBar. -->
    <style name="Theme.Tracker" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/gv_common_list_background</item>
    </style>

    <style name="Theme.Tracker.Splash" parent="Theme.GeoVault.Splash">
        <item name="postSplashScreenTheme">@style/Theme.Tracker</item>
    </style>
</resources>