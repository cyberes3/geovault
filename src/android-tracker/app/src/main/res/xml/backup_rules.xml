<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude local auth/session, queue, and tracker cache data from auto backup. -->
    <exclude domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="file" path="." />
</full-backup-content>