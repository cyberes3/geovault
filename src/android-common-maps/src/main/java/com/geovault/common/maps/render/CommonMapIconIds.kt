package com.geovault.common.maps.render

object CommonMapIconIds {
    const val MARKER_DEFAULT = "gv-common-marker-default"
    const val MARKER_SELECTED = "gv-common-marker-selected"
    /** Same circular marker as [MARKER_DEFAULT], tinted to match the active navigation line. */
    const val MARKER_NAV_TARGET = "gv-common-marker-nav-target"
    const val STATION_POINT = "gv-common-station-point"
    const val STATION_POINT_SELECTED = "gv-common-station-point-selected"
    /** Same station glyph as [STATION_POINT], tinted to match the active navigation line. */
    const val STATION_POINT_NAV_TARGET = "gv-common-station-point-nav-target"
}
