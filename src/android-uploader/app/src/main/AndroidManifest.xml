<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.geovault.common.permission.READ_SERVER_URL" />

    <application
        android:name=".UploaderApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeoVaultUploader"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GeoVaultUploader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.GeoVaultUploader"
            android:parentActivityName=".MainActivity" />
        <provider
            android:name="com.geovault.common.ServerUrlProvider"
            android:authorities="com.geovault.uploader.serverurl"
            android:exported="true"
            android:permission="com.geovault.common.permission.READ_SERVER_URL" />
        <!-- Handle both single and multiple file shares -->
        <activity
            android:name=".OAuthCallbackActivity"
            android:exported="true"
            android:theme="@style/Theme.GeoVaultUploader"
            android:launchMode="singleTask"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.geovault.uploader" android:host="oauth" android:pathPrefix="/callback" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MultiUploadActivity"
            android:exported="true"
            android:theme="@style/Theme.GeoVaultUploader">
            <!-- Handle single file shares - Accept all files, validation happens in code -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <!-- Handle multiple file shares - Accept all files, validation happens in code -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>
    </application>

</manifest>

