<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Platform has no android:Theme.Material.DayNight.*; AppCompat provides DayNight + NoActionBar. -->
    <style name="Theme.Uploader" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/gv_common_list_background</item>
    </style>

    <style name="Theme.Uploader.Splash" parent="Theme.GeoVault.Splash">
        <item name="postSplashScreenTheme">@style/Theme.Uploader</item>
    </style>
</resources>