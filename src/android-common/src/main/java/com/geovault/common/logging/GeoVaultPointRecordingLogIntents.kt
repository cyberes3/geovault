package com.geovault.common.logging

/**
 * Example: `adb shell am broadcast -a com.geovault.common.EXPORT_POINT_RECORDING_LOG -p <package>`
 */
object GeoVaultPointRecordingLogIntents {
    const val ACTION_EXPORT_POINT_RECORDING_LOG = "com.geovault.common.EXPORT_POINT_RECORDING_LOG"
}
