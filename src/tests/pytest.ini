[pytest]
DJANGO_SETTINGS_MODULE = website.settings
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = .
addopts = 
    --reuse-db
    --ds=website.settings
    -v
    -p pytest_django_version_fix
filterwarnings =
    ignore::DeprecationWarning:channels_redis


