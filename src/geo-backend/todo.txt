insane linework test isn't showing up on the map
Error message on map when loading fails
Error message on map when an exception is encountered
Allow skipping items during import
please download the assets on the allauth templates and store them locally and don't use CDNs

Later:
For tracks, set the created date to the timestamp of the first point in the track
Allow edit raw jspn in feature edit page or map view
See how https://trails.colorado.gov loads their data in the map
Add a config route so the server can send config settings stored in the config yaml to the frontend
Look for magic numbers and strings and move those to a yaml config
Debug log messages are hidden by default and only shown if a user enables that in his settings
validate that `FILE_TYPE_CONFIGS[file_type].max_size` is always less than 200 or else the database will break
